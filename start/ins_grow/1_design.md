
### **项目名称：AI Instagram 运营助手 (代号: "InstaGrow AI")**

### **项目目标**
打造一个智能AI Agent，旨在自动化和优化跨境电商在Instagram上的营销活动，通过精准的竞品分析、高质量的内容生成和高效的目标用户互动，最终实现品牌曝光提升和电商网站引流。

### **核心功能模块设计**

我们将整个系统划分为三个相互关联的核心模块：

1.  **市场与竞品洞察模块 (Insight Engine)**
2.  **内容创作与规划模块 (Content Factory)**
3.  **用户增长与互动模块 (Growth Engine)**

---

#### **模块一：市场与竞品洞察模块 (Insight Engine)**

该模块是所有策略的起点，负责“知己知彼”。

*   **输入：** 运营者提供的核心信息，如：
    *   产品类目 (例如: "手工皮革钱包", "户外露营装备", "瑜伽服饰")
    *   目标市场/国家 (例如: "美国", "欧洲", "东南亚")
    *   品牌关键词

*   **功能细分：**

    1.  **竞品账户发现 (Competitor Discovery):**
        *   **搜索机制:** AI Agent利用关键词、热门标签 (#handmadeleather, #campinggear) 和地理位置标签，在Instagram上搜索并识别出Top 10-20的直接或间接竞品账户。
        *   **筛选标准:** 根据粉丝数、互动率、发帖频率和内容相关性对搜索结果进行排序和筛选。
        *   **输出:** 一个可交互的竞品账户列表。

    2.  **竞品策略深度分析 (Competitor Strategy Analysis):**
        *   **账户定位分析 (Profile Analysis):**
            *   **Bio分析:** 提取竞品的Bio文案，分析其价值主张、关键词和Call-to-Action (行动号召，如Link in Bio)。
            *   **链接分析:** 追踪其Bio中的链接（如Linktree, Shopify店铺），了解其引流路径。
        *   **内容策略分析 (Content Analysis):**
            *   **帖子类型:** 统计Reels、Carousel（轮播图）、单图、Story的发布比例。
            *   **热门内容:** 识别出竞品互动量（点赞、评论、分享）最高的帖子，分析其主题、形式和文案风格。
            *   **发帖频率与时段:** 分析竞品的发帖规律，找出其目标受众最活跃的时间。
        *   **标签策略分析 (Hashtag Analysis):**
            *   **标签云:** 提取并分类竞品使用的所有Hashtag（品牌标签、行业标签、热门标签、长尾标签）。
            *   **有效性评估:** 分析不同标签下的帖子互动表现，推荐高性价比的标签组合。
        *   **粉丝画像分析 (Audience Analysis - 推断):**
            *   分析活跃粉丝（评论者）的公开信息，推断其兴趣、地域和语言，形成初步的用户画像。

*   **输出：**
    *   一份可视化的《Instagram竞品分析报告》，包含图表和关键洞察。
    *   一个推荐的“基础策略档案”，包括：建议的账户Bio模板、核心Hashtag列表、推荐发帖时间和内容方向。

---

#### **模块二：内容创作与规划模块 (Content Factory)**

该模块是吸引用户的核心，负责“生产弹药”。

*   **输入：**
    *   模块一的《基础策略档案》。
    *   运营者提供的产品信息（图片、描述、卖点、价格、链接）。
    *   期望的品牌调性（例如：专业、有趣、奢华、简约）。

*   **功能细分：**

    1.  **内容日历规划 (Content Calendar Planning):**
        *   AI根据竞品分析和最佳实践，自动生成一个为期一周或一个月的发帖日历建议。
        *   日历中会规划好每天的帖子主题和类型（例如：周一新品展示、周三用户故事、周五教程Reel）。

    2.  **AI文案生成 (AI Caption Generation):**
        *   **多风格写作:** 根据设定的品牌调性，为单个产品或主题生成3-5个不同风格的文案版本（如故事型、痛点型、利益驱动型）。
        *   **结构化文案:** 文案自动包含吸引人的开头、产品介绍、情感共鸣点和明确的CTA（如 "Shop now via the link in our bio!"）。
        *   **智能标签推荐:** 结合文案内容和模块一的分析，自动推荐一组最佳的Hashtag组合。

    3.  **AI配图生成与优化 (AI Visuals Generation):**
        *   **模式一：AI原创视觉 (For Lifestyle/Concept):**
            *   当缺乏素材时，AI可以根据产品描述和场景需求（如“一个放在沙滩上的手工皮革钱包，夕阳背景”），使用DALL-E 3或Midjourney等模型生成高质量的场景图、氛围图。这对于提升品牌美学非常有用。
        *   **模式二：产品图优化与模板化 (For Product Showcase):**
            *   运营者上传产品白底图。
            *   AI自动抠图，并将其置入预设的、符合品牌风格的模板中（如添加品牌Logo、背景、装饰性元素）。
            *   AI可以对现有图片进行风格化处理或色彩增强。

*   **输出：**
    *   一个可视化的内容日历。
    *   每日的“待发布内容包”，包含：最终图片/视频、精选文案、推荐标签。运营者只需一键审核和复制。

---

#### **模块三：用户增长与互动模块 (Growth Engine)**

该模块是实现引流的关键，负责“精准出击”。

*   **输入：**
    *   模块一确定的竞品账户和目标用户画像。
    *   每日互动预算（例如：30-50个用户）。

*   **功能细分：**

    1.  **目标用户发现 (Target User Discovery):**
        *   **来源一（竞品粉丝）:** 筛选竞品账户下近期活跃的粉丝（例如，最近评论或点赞的人）。
        *   **来源二（标签用户）:** 抓取近期使用了特定高意向度标签（如 #iwantthis, #shoplocalhandmade）的用户。
        *   **来源三（互动用户）:** 找到在竞品热门帖子上发表了积极评论的用户。
        *   **用户过滤:** AI会自动过滤掉机器人账户、僵尸粉或非目标市场的用户。

    2.  **智能化模拟互动 (Intelligent & Human-like Interaction):**
        *   **安全机制:**
            *   **随机化操作:** 在一天内随机分布互动行为，避免在短时间内集中操作。
            *   **每日限额:** 严格遵守每日30-50次的互动上限，避免被Instagram判定为机器人。
        *   **互动策略:**
            *   **点赞 (Like):** 对目标用户的最新1-2篇帖子进行点赞。这是最轻量级的互动。
            *   **关注 (Follow):** 关注高质量的潜在用户，吸引其回访主页。
            *   **智能评论 (AI-Powered Commenting):**
                *   **核心技术:** 使用多模态大模型（如GPT-4V），让AI能够“看懂”用户的图片内容。
                *   **生成非模板化评论:** AI会根据图片内容生成具体、真诚的评论。
                    *   *示例（用户发了一张在登山的照片）:*
                        *   **劣质评论:** "Nice pic!"
                        *   **AI智能评论:** "Wow, that view is breathtaking! Where was this trail? Looks like an amazing hike."
                *   **评论审核:** 为保证品牌安全，所有AI生成的评论会进入一个“待批准”列表，由运营者一键确认后发出。

*   **输出：**
    *   每日互动日志，记录今天互动了哪些用户、采取了什么行动。
    *   增长报告，追踪由互动带来的新粉丝增长、主页访问量和链接点击量。

### **系统架构与工作流程**

1.  **用户界面 (Dashboard):**
    *   一个简洁的Web界面，运营者在此输入产品信息、审核AI生成的内容和互动、查看报告。

2.  **后端服务 (Backend):**
    *   接收前端请求，调用各大模型API，处理业务逻辑。
    *   管理数据库，存储竞品数据、内容库、互动日志等。

3.  **AI核心 (AI Core):**
    *   **LLM (大语言模型):** 如GPT-4/4o，用于文案生成、策略分析。
    *   **VLM (视觉语言模型):** 如GPT-4V，用于智能评论生成。
    *   **Image Generation Model:** 如DALL-E 3 API，用于图片生成。

4.  **数据接口/爬虫 (Data Interface):**
    *   **关键与风险点:** Instagram官方API对上述许多功能（特别是用户发现和互动）限制严格。因此，这部分可能需要依赖第三方API服务或自建的、遵守robots.txt规则并采用高度模拟人类行为的爬虫（使用Selenium/Playwright等工具），**并必须向用户明确其中的账户风险**。

### **工作流程示意**

**启动阶段:**
1.  **运营者** 在Dashboard输入产品类目。
2.  **Agent** 启动 **模块一**，进行市场分析，生成《竞品分析报告》。
3.  **运营者** 审核报告，确认基础策略。

**日常运营:**
4.  **Agent** 基于策略和产品信息，启动 **模块二**，生成未来一周的内容日历和每日待发布内容包。
5.  **运营者** 每天登录Dashboard，审核并微调当日的图文内容，然后手动发布或使用第三方工具定时发布。
6.  **Agent** 启动 **模块三**，发现目标用户，并生成智能评论进入“待批准”列表。
7.  **运营者** 快速审核评论，一键批准。
8.  **Agent** 在一天中分时段、随机地执行点赞、关注和已批准的评论操作。
9.  **Agent** 每日生成运营简报，展示粉丝增长、互动数据和引流链接点击情况。

---

这个设计方案兼顾了AI的自动化效率和人工审核的安全性与品牌控制力，形成了一个“AI主导，人机协同”的高效运营模式。下一步可以基于这个设计进行技术选型和原型开发。

好的，这是一个非常棒的AI Agent应用场景。为跨境电商引流的Instagram运营助手，其核心是**效率**、**精准**和**增长**。

下面，我将为您设计一个详细的功能方案，分为三大核心模块，并阐述其系统架构、技术实现和工作流程。

---

### **项目名称：AI Instagram 运营助手 (代号: "InstaGrow AI")**

### **项目目标**
打造一个智能AI Agent，旨在自动化和优化跨境电商在Instagram上的营销活动，通过精准的竞品分析、高质量的内容生成和高效的目标用户互动，最终实现品牌曝光提升和电商网站引流。

### **核心功能模块设计**

我们将整个系统划分为三个相互关联的核心模块：

1.  **市场与竞品洞察模块 (Insight Engine)**
2.  **内容创作与规划模块 (Content Factory)**
3.  **用户增长与互动模块 (Growth Engine)**

---

#### **模块一：市场与竞品洞察模块 (Insight Engine)**

该模块是所有策略的起点，负责“知己知彼”。

*   **输入：** 运营者提供的核心信息，如：
    *   产品类目 (例如: "手工皮革钱包", "户外露营装备", "瑜伽服饰")
    *   目标市场/国家 (例如: "美国", "欧洲", "东南亚")
    *   品牌关键词

*   **功能细分：**

    1.  **竞品账户发现 (Competitor Discovery):**
        *   **搜索机制:** AI Agent利用关键词、热门标签 (#handmadeleather, #campinggear) 和地理位置标签，在Instagram上搜索并识别出Top 10-20的直接或间接竞品账户。
        *   **筛选标准:** 根据粉丝数、互动率、发帖频率和内容相关性对搜索结果进行排序和筛选。
        *   **输出:** 一个可交互的竞品账户列表。

    2.  **竞品策略深度分析 (Competitor Strategy Analysis):**
        *   **账户定位分析 (Profile Analysis):**
            *   **Bio分析:** 提取竞品的Bio文案，分析其价值主张、关键词和Call-to-Action (行动号召，如Link in Bio)。
            *   **链接分析:** 追踪其Bio中的链接（如Linktree, Shopify店铺），了解其引流路径。
        *   **内容策略分析 (Content Analysis):**
            *   **帖子类型:** 统计Reels、Carousel（轮播图）、单图、Story的发布比例。
            *   **热门内容:** 识别出竞品互动量（点赞、评论、分享）最高的帖子，分析其主题、形式和文案风格。
            *   **发帖频率与时段:** 分析竞品的发帖规律，找出其目标受众最活跃的时间。
        *   **标签策略分析 (Hashtag Analysis):**
            *   **标签云:** 提取并分类竞品使用的所有Hashtag（品牌标签、行业标签、热门标签、长尾标签）。
            *   **有效性评估:** 分析不同标签下的帖子互动表现，推荐高性价比的标签组合。
        *   **粉丝画像分析 (Audience Analysis - 推断):**
            *   分析活跃粉丝（评论者）的公开信息，推断其兴趣、地域和语言，形成初步的用户画像。

*   **输出：**
    *   一份可视化的《Instagram竞品分析报告》，包含图表和关键洞察。
    *   一个推荐的“基础策略档案”，包括：建议的账户Bio模板、核心Hashtag列表、推荐发帖时间和内容方向。

---

#### **模块二：内容创作与规划模块 (Content Factory)**

该模块是吸引用户的核心，负责“生产弹药”。

*   **输入：**
    *   模块一的《基础策略档案》。
    *   运营者提供的产品信息（图片、描述、卖点、价格、链接）。
    *   期望的品牌调性（例如：专业、有趣、奢华、简约）。

*   **功能细分：**

    1.  **内容日历规划 (Content Calendar Planning):**
        *   AI根据竞品分析和最佳实践，自动生成一个为期一周或一个月的发帖日历建议。
        *   日历中会规划好每天的帖子主题和类型（例如：周一新品展示、周三用户故事、周五教程Reel）。

    2.  **AI文案生成 (AI Caption Generation):**
        *   **多风格写作:** 根据设定的品牌调性，为单个产品或主题生成3-5个不同风格的文案版本（如故事型、痛点型、利益驱动型）。
        *   **结构化文案:** 文案自动包含吸引人的开头、产品介绍、情感共鸣点和明确的CTA（如 "Shop now via the link in our bio!"）。
        *   **智能标签推荐:** 结合文案内容和模块一的分析，自动推荐一组最佳的Hashtag组合。

    3.  **AI配图生成与优化 (AI Visuals Generation):**
        *   **模式一：AI原创视觉 (For Lifestyle/Concept):**
            *   当缺乏素材时，AI可以根据产品描述和场景需求（如“一个放在沙滩上的手工皮革钱包，夕阳背景”），使用DALL-E 3或Midjourney等模型生成高质量的场景图、氛围图。这对于提升品牌美学非常有用。
        *   **模式二：产品图优化与模板化 (For Product Showcase):**
            *   运营者上传产品白底图。
            *   AI自动抠图，并将其置入预设的、符合品牌风格的模板中（如添加品牌Logo、背景、装饰性元素）。
            *   AI可以对现有图片进行风格化处理或色彩增强。

*   **输出：**
    *   一个可视化的内容日历。
    *   每日的“待发布内容包”，包含：最终图片/视频、精选文案、推荐标签。运营者只需一键审核和复制。

---

#### **模块三：用户增长与互动模块 (Growth Engine)**

该模块是实现引流的关键，负责“精准出击”。

*   **输入：**
    *   模块一确定的竞品账户和目标用户画像。
    *   每日互动预算（例如：30-50个用户）。

*   **功能细分：**

    1.  **目标用户发现 (Target User Discovery):**
        *   **来源一（竞品粉丝）:** 筛选竞品账户下近期活跃的粉丝（例如，最近评论或点赞的人）。
        *   **来源二（标签用户）:** 抓取近期使用了特定高意向度标签（如 #iwantthis, #shoplocalhandmade）的用户。
        *   **来源三（互动用户）:** 找到在竞品热门帖子上发表了积极评论的用户。
        *   **用户过滤:** AI会自动过滤掉机器人账户、僵尸粉或非目标市场的用户。

    2.  **智能化模拟互动 (Intelligent & Human-like Interaction):**
        *   **安全机制:**
            *   **随机化操作:** 在一天内随机分布互动行为，避免在短时间内集中操作。
            *   **每日限额:** 严格遵守每日30-50次的互动上限，避免被Instagram判定为机器人。
        *   **互动策略:**
            *   **点赞 (Like):** 对目标用户的最新1-2篇帖子进行点赞。这是最轻量级的互动。
            *   **关注 (Follow):** 关注高质量的潜在用户，吸引其回访主页。
            *   **智能评论 (AI-Powered Commenting):**
                *   **核心技术:** 使用多模态大模型（如GPT-4V），让AI能够“看懂”用户的图片内容。
                *   **生成非模板化评论:** AI会根据图片内容生成具体、真诚的评论。
                    *   *示例（用户发了一张在登山的照片）:*
                        *   **劣质评论:** "Nice pic!"
                        *   **AI智能评论:** "Wow, that view is breathtaking! Where was this trail? Looks like an amazing hike."
                *   **评论审核:** 为保证品牌安全，所有AI生成的评论会进入一个“待批准”列表，由运营者一键确认后发出。

*   **输出：**
    *   每日互动日志，记录今天互动了哪些用户、采取了什么行动。
    *   增长报告，追踪由互动带来的新粉丝增长、主页访问量和链接点击量。

### **系统架构与工作流程**

1.  **用户界面 (Dashboard):**
    *   一个简洁的Web界面，运营者在此输入产品信息、审核AI生成的内容和互动、查看报告。

2.  **后端服务 (Backend):**
    *   接收前端请求，调用各大模型API，处理业务逻辑。
    *   管理数据库，存储竞品数据、内容库、互动日志等。

3.  **AI核心 (AI Core):**
    *   **LLM (大语言模型):** 如GPT-4/4o，用于文案生成、策略分析。
    *   **VLM (视觉语言模型):** 如GPT-4V，用于智能评论生成。
    *   **Image Generation Model:** 如DALL-E 3 API，用于图片生成。

4.  **数据接口/爬虫 (Data Interface):**
    *   **关键与风险点:** Instagram官方API对上述许多功能（特别是用户发现和互动）限制严格。因此，这部分可能需要依赖第三方API服务或自建的、遵守robots.txt规则并采用高度模拟人类行为的爬虫（使用Selenium/Playwright等工具），**并必须向用户明确其中的账户风险**。

### **工作流程示意**

**启动阶段:**
1.  **运营者** 在Dashboard输入产品类目。
2.  **Agent** 启动 **模块一**，进行市场分析，生成《竞品分析报告》。
3.  **运营者** 审核报告，确认基础策略。

**日常运营:**
4.  **Agent** 基于策略和产品信息，启动 **模块二**，生成未来一周的内容日历和每日待发布内容包。
5.  **运营者** 每天登录Dashboard，审核并微调当日的图文内容，然后手动发布或使用第三方工具定时发布。
6.  **Agent** 启动 **模块三**，发现目标用户，并生成智能评论进入“待批准”列表。
7.  **运营者** 快速审核评论，一键批准。
8.  **Agent** 在一天中分时段、随机地执行点赞、关注和已批准的评论操作。
9.  **Agent** 每日生成运营简报，展示粉丝增长、互动数据和引流链接点击情况。

---

这个设计方案兼顾了AI的自动化效率和人工审核的安全性与品牌控制力，形成了一个“AI主导，人机协同”的高效运营模式。下一步可以基于这个设计进行技术选型和原型开发。

好的，遵照您的指示，我们正式确定 "InstaGrow AI" 项目的技术选型方案。

以下是根据我们之前讨论的推荐方案，最终确定的技术栈。这套方案旨在实现**开发效率、AI能力、系统稳定性和风险控制**的最佳平衡。

---

### **"InstaGrow AI" 项目最终技术选型方案**

#### **1. 前端 (Frontend Dashboard)**

*   **框架:** **React (使用 Next.js)**
    *   利用其文件系统路由、API路由和服务端渲染能力，构建功能强大且SEO友好的管理后台。
*   **UI 库与样式:** **Tailwind CSS + Shadcn/ui**
    *   使用 Tailwind CSS 进行快速、原子化的样式开发。结合 Shadcn/ui 组件库，可以快速构建出美观、一致且高度可定制的界面。
*   **状态管理:** **Zustand**
    *   一个轻量、简洁的全局状态管理库，API友好，能有效管理复杂的前端状态。
*   **数据请求:** **React Query (TanStack Query)**
    *   用于处理所有与后端API的数据交互，提供缓存、后台更新、重试等强大功能，提升用户体验。

#### **2. 后端 (Backend Service)**

*   **语言:** **Python 3.11+**
*   **Web 框架:** **FastAPI**
    *   利用其原生的异步特性处理高并发的API调用（如请求OpenAI、Instagram数据接口），并通过Pydantic进行严格的数据校验。
*   **ORM (对象关系映射):** **SQLModel**
    *   由FastAPI作者创建，完美结合了SQLAlchemy和Pydantic，可以用一套代码模型同时定义数据库表结构和API数据结构，极大提升开发效率。

#### **3. AI 核心 (AI Core)**

*   **主要服务商:** **OpenAI API**
*   **核心模型 (文案、策略、评论):** **GPT-4o**
    *   利用其顶级的语言理解、逻辑推理能力以及内置的视觉能力（Vision），实现高质量文案生成和“看图评论”的核心功能。其速度和成本效益也优于旧版模型。
*   **图像生成模型:** **DALL-E 3**
    *   通过API调用，根据文本描述生成高质量的品牌氛围图和产品场景图。

#### **4. 数据接口 (Instagram Interaction)**

*   **首选方案 (生产环境):** **第三方商业API服务 (例如：Apify)**
    *   **选定理由:** **风险控制优先**。这是整个项目中最关键的决策。通过专业的第三方服务来处理Instagram的数据抓取和模拟互动，可以最大限度地规避账户被封禁的风险，保证服务的稳定性。我们将购买其API服务，专注于上层业务逻辑开发。
*   **备用方案 (内部研发):** **Python + Playwright**
    *   此方案仅用于内部研究或在第三方API不可用时的紧急备选，**不直接用于生产环境的客户账户**，以避免不可控的风险。

#### **5. 数据库 (Database)**

*   **数据库系统:** **PostgreSQL (版本 15+)**
    *   利用其成熟的关系型数据管理能力存储结构化数据（用户信息、竞品列表等），并大量使用其强大的 **JSONB** 字段类型来存储非结构化的AI返回结果和帖子元数据。

#### **6. 部署与运维 (Deployment & DevOps)**

*   **容器化:** **Docker**
    *   将后端FastAPI应用打包成标准化的Docker镜像，确保环境一致性。
*   **前端部署:** **Vercel**
    *   与GitHub仓库直接集成，实现CI/CD自动化，每次代码推送即可自动部署前端应用。
*   **后端部署:** **AWS Fargate**
    *   使用AWS的Serverless容器服务，按需运行后端Docker容器，无需管理底层服务器，具备高可用和弹性伸缩能力。
*   **数据库托管:** **AWS RDS for PostgreSQL**
    *   使用亚马逊的托管数据库服务，自动处理备份、扩展、安全和维护工作。
*   **CI/CD:** **GitHub Actions**
    *   构建自动化工作流，用于测试、打包Docker镜像并推送到AWS ECR（容器镜像仓库），最终触发Fargate部署。

---

### **技术选型总结表**

| 模块 | 最终选定技术 |
| :--- | :--- |
| **前端框架** | React (Next.js) |
| **前端样式** | Tailwind CSS + Shadcn/ui |
| **后端框架** | Python + FastAPI |
| **AI 核心模型** | OpenAI API (GPT-4o, DALL-E 3) |
| **数据接口** | **Apify (第三方商业API)** |
| **数据库** | PostgreSQL (使用JSONB) |
| **容器化** | Docker |
| **云服务与部署**| Vercel (前端), AWS Fargate + RDS (后端/数据库) |

这套技术栈是现代化、高生产力且可扩展的组合。我们已经完成了技术选型阶段，下一步可以开始进行**系统架构设计和数据库表结构设计**。


好的，我们已经完成了技术选型。现在，让我们进入核心的**系统架构设计**和**数据库表结构设计**阶段。

---

### **一、 系统架构设计 (System Architecture)**

本架构遵循**分层、解耦、高内聚**的原则，确保系统的可扩展性、可维护性和稳定性。

#### **1. 架构图**

```mermaid
graph TD
    subgraph "用户端"
        User[👨‍💻 运营者] --> Browser[🌐 浏览器]
    end

    subgraph "前端 (Vercel)"
        Browser -- 1. HTTPS Request --> NextJS[Next.js 应用]
    end

    subgraph "AWS 云平台"
        APIGateway[API Gateway] -- 3. 路由请求 --> Fargate[AWS Fargate]
        Fargate -- 4. 业务逻辑处理 --> DB[AWS RDS for PostgreSQL]
        Fargate -- 5. AI任务 --> OpenAI
        Fargate -- 6. Instagram数据 --> Apify
        
        subgraph "后端服务 (Docker on Fargate)"
            FastAPI[FastAPI 应用]
        end
        
        Fargate -- 包含 --> FastAPI

        Scheduler[AWS EventBridge Scheduler] -- 7. 定时触发 --> APIGateway

        ECR[ECR 容器镜像仓库]
    end

    subgraph "第三方服务"
        OpenAI[🤖 OpenAI API <br>(GPT-4o, DALL-E 3)]
        Apify[🕷️ Apify API <br>(Instagram Scraper)]
    end

    subgraph "开发与部署"
        Dev[开发人员] -- git push --> GitHub[GitHub 仓库]
        GitHub -- 触发 CI/CD --> GHActions[GitHub Actions]
        GHActions -- a. 部署前端 --> NextJS
        GHActions -- b. 构建镜像 & 推送 --> ECR
        GHActions -- c. 更新服务 --> Fargate
    end

    NextJS -- 2. API 调用 --> APIGateway

    style User fill:#f9f,stroke:#333,stroke-width:2px
    style DB fill:#cde,stroke:#333,stroke-width:2px
```

#### **2. 核心组件说明**

*   **用户端 (User-End):**
    *   **运营者 (User):** 系统的最终用户，通过浏览器访问我们的服务。
*   **前端 (Frontend on Vercel):**
    *   **Next.js 应用:** 托管在Vercel上的React应用。负责提供所有用户界面，包括仪表盘、内容编辑器、审批列表和分析报告。它是一个纯粹的表示层，通过API与后端通信。
*   **AWS 云平台 (Backend & Infrastructure):**
    *   **API Gateway:** 作为所有后端服务的统一入口。负责请求路由、身份验证（JWT）、速率限制和日志记录，保护后端服务。
    *   **AWS Fargate:** Serverless容器运行服务。我们的FastAPI后端应用被打包成Docker镜像，在此运行。Fargate会根据负载自动伸缩容器实例，我们无需管理服务器。
    *   **AWS RDS for PostgreSQL:** 托管的关系型数据库服务。存储所有业务数据，由AWS负责维护、备份和扩展。
    *   **AWS EventBridge Scheduler:** 定时任务服务。用于触发周期性任务，例如“每日抓取竞品数据”、“每日发现目标用户”等。它会定时调用API Gateway的特定端点。
*   **第三方服务 (External Services):**
    *   **OpenAI API:** AI核心。后端服务将调用此API来完成文案生成、策略分析、图像生成和智能评论等任务。
    *   **Apify API:** Instagram数据接口。后端服务调用此API来执行所有与Instagram相关的操作，如获取竞品帖子、搜索用户、点赞、评论等。**这是规避直接与Instagram交互风险的关键。**
*   **开发与部署 (CI/CD):**
    *   **GitHub & GitHub Actions:** 代码托管和自动化工作流。当开发者提交代码到主分支时，GitHub Actions会自动触发：
        1.  对前端，执行构建并部署到Vercel。
        2.  对后端，运行测试，构建Docker镜像，推送到AWS ECR，并命令Fargate使用新镜像更新服务。

#### **3. 核心数据流**

1.  **用户登录/操作:** 运营者通过浏览器访问Vercel上的Next.js应用。
2.  **前端API请求:** 应用内的操作（如点击“生成文案”）会向我们的API Gateway发送一个安全的HTTPS API请求。
3.  **后端处理:** API Gateway将请求转发给Fargate上运行的FastAPI应用。
4.  **业务逻辑:**
    *   FastAPI应用解析请求。
    *   如果需要数据库操作（如保存草稿），则与AWS RDS中的PostgreSQL交互。
    *   如果需要AI能力（如生成文案），则调用**OpenAI API**。
    *   如果需要Instagram数据（如分析竞品），则调用**Apify API**。
5.  **数据返回:** FastAPI整合所有结果，通过API Gateway返回给前端。
6.  **前端渲染:** Next.js应用接收到数据后，更新UI界面展示给用户。
7.  **定时任务:** AWS EventBridge Scheduler按预设规则（如每天凌晨2点）调用一个特定的API端点，触发后端的批量处理任务（如用户发现）。

---

### **二、 数据库表结构设计 (PostgreSQL)**

我们采用多租户设计，允许一个用户管理多个Instagram账号（工作区）。主键统一使用 `UUID` 以便未来扩展。

```sql
-- 使用 pgcrypto 扩展来生成 UUID
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- 定义 ENUM 类型以保证数据一致性
CREATE TYPE content_type AS ENUM ('POST', 'REEL', 'STORY');
CREATE TYPE content_status AS ENUM ('DRAFT', 'PENDING_APPROVAL', 'SCHEDULED', 'POSTED', 'FAILED');
CREATE TYPE interaction_type AS ENUM ('LIKE', 'COMMENT', 'FOLLOW');
CREATE TYPE interaction_status AS ENUM ('PENDING_APPROVAL', 'EXECUTED', 'FAILED', 'CANCELED');

-- 1. 用户表 (系统运营者)
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(100),
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 2. 工作区表 (用于隔离不同品牌/项目)
CREATE TABLE workspaces (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    name VARCHAR(100) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 3. 绑定的Instagram账号表
CREATE TABLE instagram_accounts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    workspace_id UUID NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
    username VARCHAR(100) UNIQUE NOT NULL,
    instagram_user_id VARCHAR(100), -- Instagram的数字ID
    product_category TEXT,
    brand_persona TEXT, -- 品牌人设描述，用于AI生成内容
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 4. 竞品账户表
CREATE TABLE competitors (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    workspace_id UUID NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE,
    username VARCHAR(100) NOT NULL,
    analysis_results JSONB, -- 存储完整的竞品分析报告 (Profile, 内容策略, 标签等)
    last_analyzed_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (workspace_id, username)
);

-- 5. 内容创作表 (核心)
CREATE TABLE content_pieces (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    instagram_account_id UUID NOT NULL REFERENCES instagram_accounts(id) ON DELETE CASCADE,
    type content_type NOT NULL,
    status content_status NOT NULL DEFAULT 'DRAFT',
    
    -- AI生成的核心内容
    generated_caption TEXT,
    generated_image_url TEXT, -- DALL-E生成的图片URL
    suggested_hashtags TEXT[], -- 推荐的标签数组
    
    -- 运营者最终确认的内容
    final_caption TEXT,
    final_media_url TEXT, -- 可能是AI图，也可能是用户上传的图
    
    -- 调度与发布信息
    scheduled_at TIMESTAMPTZ,
    posted_at TIMESTAMPTZ,
    post_url VARCHAR(255), -- 发布后回填的帖子链接
    error_message TEXT, -- 如果发布失败
    
    ai_raw_response JSONB, -- 存储OpenAI返回的原始JSON，便于调试和追溯
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 6. 互动日志表
CREATE TABLE interactions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    instagram_account_id UUID NOT NULL REFERENCES instagram_accounts(id) ON DELETE CASCADE, -- 执行互动的我方账号
    target_username VARCHAR(100) NOT NULL, -- 互动目标的用户名
    target_post_url VARCHAR(255), -- 互动的帖子URL (如果是评论或点赞)
    
    type interaction_type NOT NULL,
    status interaction_status NOT NULL DEFAULT 'PENDING_APPROVAL',
    
    content TEXT, -- 评论内容
    ai_generated_comment TEXT, -- AI生成的原始评论，供审核
    
    executed_at TIMESTAMPTZ,
    error_message TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 7. 分析快照表 (用于生成报告)
CREATE TABLE analytics_snapshots (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    instagram_account_id UUID NOT NULL REFERENCES instagram_accounts(id) ON DELETE CASCADE,
    snapshot_date DATE NOT NULL,
    followers_count INT,
    following_count INT,
    posts_count INT,
    -- 可以从Link in Bio工具的API获取，或者估算
    profile_views INT,
    website_clicks INT,
    
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (instagram_account_id, snapshot_date)
);

-- 创建索引以优化查询性能
CREATE INDEX idx_workspaces_user_id ON workspaces(user_id);
CREATE INDEX idx_ig_accounts_workspace_id ON instagram_accounts(workspace_id);
CREATE INDEX idx_competitors_workspace_id ON competitors(workspace_id);
CREATE INDEX idx_content_pieces_account_id_status ON content_pieces(instagram_account_id, status);
CREATE INDEX idx_interactions_account_id_status ON interactions(instagram_account_id, status);
CREATE INDEX idx_analytics_account_id_date ON analytics_snapshots(instagram_account_id, snapshot_date DESC);
```

